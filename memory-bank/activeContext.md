# Active Context: Chess Tracker

## Текущее состояние проекта
Проект находится в **начальной стадии**. Создана только базовая документация:
- `project_task.md` — полное техническое задание
- `.gitignore` — игнорирование `homework_requirements.md`
- `.clineignore` — пустой
- `.clinerules/` — правила ведения REPORT.md и документации
- `Memory Bank` — инициализируется сейчас

Код приложения отсутствует. CHANGES.md и PROMPTS.md пусты.

## Последние изменения
- Инициализация репозитория (git init)
- Создание dotfiles (.gitignore, .clineignore, .clinerules/)
- Создание project_task.md с полным ТЗ
- Инициализация Memory Bank (текущий шаг)

## Следующие шаги (приоритетный порядок)
1. **Создать ARCHITECTURE.md** — описание архитектуры, ERD, план разработки (требование ТЗ — до кодирования)
2. **Настроить Docker-окружение** — docker-compose.yml с сервисами (backend, db, telegram-bot)
3. **Настроить управление зависимостями через uv** — pyproject.toml
4. **Реализовать backend**:
   - Модели SQLAlchemy (игроки, турниры, партии, рейтинговая история, пользователи, избранное, лог активности)
   - Миграции Alembic
   - API эндпоинты (CRUD для всех сущностей)
   - Аутентификация JWT
   - SSE
   - Экспорт/импорт CSV
5. **Реализовать frontend**:
   - Jinja2 шаблоны
   - HTMX для динамики
   - Alpine.js для реактивности
   - Chart.js для графиков
6. **Реализовать Telegram-bot**
7. **Наполнить seed-данные**
8. **Написать тесты** (минимум 10)
9. **Настроить CI** (GitHub Actions)
10. **Заполнить документацию** (README.md, REPORT.md)

## Активные решения и considerations
- **Структура проекта**: монорепозиторий с backend, frontend (шаблоны внутри backend), telegram-bot как отдельный сервис
- **Фронтенд**: без отдельного сервера — шаблоны Jinja2 отдаются FastAPI, HTMX делает запросы к тем же эндпоинтам
- **Docker**: три контейнера — backend (FastAPI + шаблоны), db (PostgreSQL), telegram-bot
- **Telegram-bot**: отдельный микросервис в том же compose-файле, общается с backend через HTTP/REST
- **Аутентификация**: JWT токены, два предзаполненных аккаунта (admin, user)

## Изученные паттерны и предпочтения
- Управление зависимостями через `uv` (не pip/poetry)
- Линтер `ruff` (flake8 + isort + pyupgrade в одном)
- Все промпты и изменения документируются в PROMPTS.md и CHANGES.md
- Проект должен запускаться одной командой `docker compose up`