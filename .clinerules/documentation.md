# Правило документирования проекта

## История промптов (PROMPTS.md)

**Каждый промпт от пользователя сохраняется в `PROMPTS.md` — без исключений.**

Порядок действий:
1. Получил промпт от пользователя
2. Сразу (до выполнения) добавь запись в конец PROMPTS.md
3. Выполни задачу
4. Дополни запись результатом

Формат записи:
```markdown
## YYYY-MM-DD HH:MM — Краткое описание

**Режим:** Plan / Act / Plan → Act

**Промпт пользователя:**
```
(текст промпта)
```

**Ответ/план (Plan mode):** (если был)
- ...

**Результат:**
- ...
```

**Важно:**
- Запись создаётся **сразу после получения промпта**, а не в конце майлстоуна
- Серия промптов в одной сессии = отдельная запись на каждый промпт (или одна запись с подразделами, если промпты тесно связаны)
- Не оговаривай "если был новый промпт" — каждый промпт = запись

## История изменений (CHANGES.md)

Все изменения документируются в `CHANGES.md`:
- Дата, время (YYYY-MM-DD HH:MM)
- Описание изменений
- Созданные/обновлённые файлы
- Записи добавляются в **конец файла**

## Отчёт (REPORT.md)

Записи в «История работы» после каждого завершённого шага. При проблемах — в «Ключевые проблемы и решения» и «Удачные/неудачные шаги».

## Правило обновления

PROMPTS.md, CHANGES.md и REPORT.md **обновляются после каждого завершённого шага** (одного tool use или группы связанных tool use):

1. После создания/изменения файлов, выполнения команд/тестов → CHANGES.md
2. Каждый промпт от пользователя → немедленно в PROMPTS.md (до выполнения задачи)
3. После завершённого шага → REPORT.md («История работы»)
4. Перед `attempt_completion` → проверить актуальность всех файлов
5. Перед `git push` → повторно проверить все файлы + Memory Bank

## Шаг консолидации

**После каждого significant изменения (добавление фичи, исправление бага, рефакторинг):**

1. Проверить, нет ли дублирующих файлов (два файла об одном и том же)
2. Проверить, нет ли устаревших ссылок (сломанные пути в memory-bank/, .clinerules/)
3. Проверить, что все цифры актуальны (количество тестов, шаблонов, скиллов)
4. Если нашёл расхождение — исправить немедленно, не откладывая
5. Если файл перемещён — обновить все ссылки на него в memory-bank/, .clinerules/, IMPLEMENTATION_PLAN.md, ARCHITECTURE.md

**Примеры консолидации:**
- `BUGS.md` в корне и `memory-bank/meta/bugs.md` = дубль → оставить один в `docs/`
- `implementation_plan.md` и `IMPLEMENTATION_PLAN.md` = дубль → удалить lowercase версию
- `REPORT_MY.MD` и `REPORT.md` = разные отчёты → оставить REPORT.md, REPORT_MY.MD переместить в `docs/`
- `memory-bank/meta/` дублирует корневые документы → удалить

**Цель:** в корне проекта — только essentials (README, ARCHITECTURE, LICENSE, основные отчёты). Всё остальное → `docs/` или `memory-bank/`.